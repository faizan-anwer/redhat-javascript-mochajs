# redhat-javascript-mochajs

## Available Scripts

In the project directory, you can run:

### `npm install`

To install dependencies in package.json file.


### `npm test`

To run mocha.js test you can use the above command.
